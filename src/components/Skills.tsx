// Deprecated: part of the generic starter scaffold, unused since the Phase 1
// rebuild (no longer imported anywhere). Contained fabricated placeholder
// skills (Three.js, Figma, etc.) that don't belong in Loay's portfolio.
// Safe to delete — left as an inert stub only because this session
// couldn't remove the file.
export {};
