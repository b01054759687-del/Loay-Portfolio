// Deprecated: part of the generic starter scaffold, unused since the Phase 1
// rebuild (no longer imported anywhere). Superseded by the mailto CTAs
// driven by src/lib/data.ts (profile.ctaSecondary). Safe to delete — left
// as an inert stub only because this session couldn't remove the file.
export {};
